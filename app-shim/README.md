# Shim API Interna Phase 1

- Endpoints:
  POST /api/generate/talking-avatar
  POST /api/generate/music-video
  POST /api/generate/character-animation
  POST /api/generate/song-generation

- Health: GET /health

- Este shim facilita pruebas cuando no hay endpoints dentro de Docker. Reemplaza por tu API real si ya la tienes.
