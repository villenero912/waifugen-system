# =============================================================================
# WaifuGen System - Environment Configuration Template
# =============================================================================
# INSTRUCCIONES:
# 1. Copia este contenido a un archivo llamado '.env' en la raíz del proyecto.
# 2. Rellena los valores con tus credenciales reales.
# 3. NUNCA compartas tu archivo .env real.
# =============================================================================

# --- GENERAL PROJECT SETTINGS ---
PROJECT_NAME=waifugen
TIMEZONE=Europe/Madrid
DEBUG=false
LOG_LEVEL=INFO
SECRET_KEY=cambia_esto_por_una_cadena_aleatoria_larga

# --- DATABASE (POSTGRES) ---
POSTGRES_DB=waifugen_prod
POSTGRES_USER=waifugen_user
POSTGRES_PASSWORD=tu_password_segura_aqui
POSTGRES_PORT=5432

# --- CACHE (REDIS) ---
REDIS_PASSWORD=otra_password_segura_aqui
REDIS_PORT=6379

# --- API KEYS (CRÍTICAS) ---
# Consigue tu key en a2e.ai
A2E_API_KEY=tu_api_key_de_a2e_aqui

# Consigue tu key en dashscope.aliyun.com (para Qwen/LLM)
QWEN_API_KEY=tu_api_key_de_qwen_aqui

# --- MONITORING (TELEGRAM) ---
# Habla con @BotFather en Telegram para crear un bot
TELEGRAM_BOT_TOKEN=tu_bot_token_aqui
TELEGRAM_CHAT_ID=tu_chat_id_personal_aqui

# --- MONITORING (GRAFANA) ---
GRAFANA_USER=admin
GRAFANA_PASSWORD=password_para_grafana_dashboard

# --- INFRAESTRUCTURA ---
APP_PORT=8000
OLLAMA_PORT=11434
# IPRoyal o similar (Phase 2 OnlyFans automation)
PROXY_URL=http://user:pass@proxy_host:port

# --- PHASE 2 MARKETING ---
WEBHOOK_URL=https://tu-dominio.com/webhook
ONLYFANS_USER=tu_usuario_of
ONLYFANS_PASS=tu_password_of
